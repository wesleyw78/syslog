# Syslog Attendance Console

Repository bootstrap for a React + Go + MySQL attendance system driven by AP syslog events.

## Layout

- `backend/` Go service
- `frontend/` Vite frontend
- `docker-compose.yml` local MySQL stack

## Status

This repository currently contains the minimal project skeleton only.
